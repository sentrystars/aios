# AIOSMac Branding

Current desktop visual direction:

- Accent color: deep green-teal
- Status glyph: `StatusGlyph.png`, derived from the same branded badge used by `AppIcon`
- App icon: generated branded placeholder set, suitable for internal builds but still worth replacing with a final production icon before release

The Xcode project now includes `Assets.xcassets` so app resources have a stable home.
